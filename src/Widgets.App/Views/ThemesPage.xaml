<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Widgets.App.Views.ThemesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="PresetItemStyle" TargetType="GridViewItem">
            <Setter Property="Margin" Value="0,0,12,12" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="MinWidth" Value="0" />
            <Setter Property="MinHeight" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
        </Style>
    </Page.Resources>

    <Grid Padding="24,16,24,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="4">
            <TextBlock Text="テーマ" Style="{StaticResource PageTitleTextStyle}" />
            <TextBlock Text="デザインのプリセットです。カードから既存のウィジェットへ適用できます。"
                       Style="{StaticResource CaptionTextStyle}" />
        </StackPanel>

        <InfoBar x:Name="InfoBarControl" Grid.Row="1" Margin="0,12,0,0" IsOpen="False" />

        <ScrollViewer Grid.Row="2" Margin="0,8,0,0" Padding="0,0,8,0">
            <StackPanel x:Name="CategoriesPanel" Padding="0,0,0,24" />
        </ScrollViewer>
    </Grid>
</Page>
